// Coordonnées de contact partagées (client-safe : aucun import serveur ici),
// réutilisées par plusieurs sections (WhatsApp, Centre d'Aide, ...).
// Numéro WhatsApp — À DÉFINIR (format international sans « + » ni espaces).
export const WHATSAPP_NUMBER = "50937123456";
